apiVersion: v1
kind: Pod
metadata:
  name: po6
  labels:
    color: red
  namespace: brk
spec:
  containers:
    - 
      name: webserver1
      image: httpd:alpine
      ports: 
        -
          containerPort: 80

